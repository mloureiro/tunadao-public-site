---
import Layout from '@layouts/Layout.astro';
import Button from '@components/Button.astro';
import { useTranslations, getLocalizedPath, type Language, defaultLang } from '@i18n/index';
import { Icon, type IconName } from '@ui/icons';

export function getStaticPaths() {
  return [
    { params: { lang: undefined } }, // / (PT - default)
    { params: { lang: 'en' } }, // /en/
  ];
}

const { lang } = Astro.params;
const currentLang: Language = (lang as Language) || defaultLang;
const t = useTranslations(currentLang);
const l = (path: string) => getLocalizedPath(path, currentLang);
const heroBackground = `${import.meta.env.BASE_URL}images/hero-bg.jpg`;

const stats = [
  { value: '1998', label: t('stats.founded') },
  { value: '25+', label: t('stats.yearsHistory') },
  { value: '60+', label: t('stats.members') },
  { value: '19', label: t('stats.citadaoEditions') },
];

const highlights: { title: string; description: string; href: string; icon: IconName }[] = [
  {
    title: t('home.sections.citadao'),
    description: t('highlights.citadaoDesc'),
    href: l('/citadao'),
    icon: 'music',
  },
  {
    title: t('home.sections.palmares'),
    description: t('highlights.palmaresDesc'),
    href: l('/palmares'),
    icon: 'trophy',
  },
  {
    title: t('home.sections.about'),
    description: t('highlights.aboutDesc'),
    href: l('/sobre'),
    icon: 'users',
  },
];
---

<Layout title={`Tunadão 1998 - ${t('home.subtitle')}`}>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero__background"></div>
    <div class="hero__content container">
      <h1 class="hero__title">{t('home.title')}</h1>
      <p class="hero__subtitle">{t('home.subtitle')}</p>
      <p class="hero__text">{t('home.heroText')}</p>
      <div class="hero__actions">
        <Button href={l('/sobre')} variant="primary" size="lg">
          {t('home.cta.about')}
        </Button>
        <Button href={l('/citadao')} variant="outline" size="lg">
          {t('home.cta.citadao')}
        </Button>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="stats">
    <div class="container">
      <div class="stats__grid">
        {
          stats.map((stat) => (
            <div class="stats__item">
              <span class="stats__value">{stat.value}</span>
              <span class="stats__label">{stat.label}</span>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Highlights Section -->
  <section class="highlights">
    <div class="container">
      <div class="highlights__grid">
        {
          highlights.map((item) => (
            <a href={item.href} class="highlight-card">
              <div class="highlight-card__icon">
                <Icon name={item.icon} size={32} />
              </div>
              <h3 class="highlight-card__title">{item.title}</h3>
              <p class="highlight-card__description">{item.description}</p>
              <span class="highlight-card__link">{t('common.readMore')} →</span>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <!-- About Preview Section -->
  <section class="about-preview">
    <div class="container">
      <div class="about-preview__content">
        <h2 class="about-preview__title">{t('about.history')}</h2>
        <p class="about-preview__text">{t('aboutPreview.text1')}</p>
        <p class="about-preview__text">{t('aboutPreview.text2')}</p>
        <Button href={l('/sobre')} variant="secondary">
          {t('home.cta.about')}
        </Button>
      </div>
    </div>
  </section>

  <!-- Music CTA Section -->
  <section class="music-cta">
    <div class="container">
      <div class="music-cta__content">
        <h2 class="music-cta__title">{t('music.title')}</h2>
        <p class="music-cta__text">{t('musicCta.text')}</p>
        <Button
          href="https://open.spotify.com/artist/7HeYIxlV5Nb1KvZkBx00sH"
          variant="primary"
          size="lg"
          target="_blank"
        >
          <Icon name="spotify" size={20} class="music-cta__icon" />
          {t('music.listenOn')}
        </Button>
      </div>
    </div>
  </section>
</Layout>

<style define:vars={{ heroBackground }}>
  /* Hero Section */
  .hero {
    position: relative;
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .hero__background {
    position: absolute;
    inset: 0;
    background-image: url(var(--heroBackground));
    background-size: cover;
    background-position: center 30%;
    z-index: -1;
  }

  .hero__background::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(
      ellipse 80% 70% at center,
      rgba(0, 0, 0, 0.3) 0%,
      rgba(0, 0, 0, 0.5) 40%,
      rgba(0, 0, 0, 0.75) 70%,
      rgba(0, 0, 0, 0.9) 100%
    );
  }

  .hero__background::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      rgba(46, 26, 71, 0.4) 0%,
      rgba(139, 0, 41, 0.2) 50%,
      rgba(46, 26, 71, 0.5) 100%
    );
  }

  .hero__content {
    text-align: center;
    color: var(--color-text-inverted);
    padding: var(--space-8) var(--space-4);
  }

  .hero__title {
    font-size: clamp(2.5rem, 8vw, 4.5rem);
    font-weight: 700;
    margin-bottom: var(--space-2);
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
  }

  .hero__subtitle {
    font-size: clamp(1.125rem, 3vw, 1.5rem);
    opacity: 0.9;
    margin-bottom: var(--space-4);
  }

  .hero__text {
    font-size: var(--text-lg);
    opacity: 0.8;
    max-width: 600px;
    margin: 0 auto var(--space-6);
  }

  .hero__actions {
    display: flex;
    gap: var(--space-3);
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Stats Section */
  .stats {
    background-color: var(--color-surface);
    padding: var(--space-8) 0;
    margin-top: -4rem;
    position: relative;
    z-index: 1;
    border-radius: var(--radius-xl) var(--radius-xl) 0 0;
  }

  .stats__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-6);
  }

  @media (min-width: 768px) {
    .stats__grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .stats__item {
    text-align: center;
  }

  .stats__value {
    display: block;
    font-size: var(--text-4xl);
    font-weight: 700;
    color: var(--color-primary);
    line-height: 1;
  }

  .stats__label {
    display: block;
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    margin-top: var(--space-1);
  }

  /* Highlights Section */
  .highlights {
    padding: var(--space-12) 0;
    background-color: var(--color-background);
  }

  .highlights__grid {
    display: grid;
    gap: var(--space-6);
  }

  @media (min-width: 768px) {
    .highlights__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .highlight-card {
    display: flex;
    flex-direction: column;
    padding: var(--space-6);
    background-color: var(--color-surface);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: inherit;
    transition:
      transform var(--transition-normal),
      box-shadow var(--transition-normal);
    border: 1px solid var(--color-border);
  }

  .highlight-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .highlight-card__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 64px;
    height: 64px;
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
    border-radius: var(--radius-lg);
    color: var(--color-text-inverted);
    margin-bottom: var(--space-4);
  }

  .highlight-card__title {
    font-size: var(--text-xl);
    font-weight: 600;
    margin-bottom: var(--space-2);
    color: var(--color-text);
  }

  .highlight-card__description {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    flex-grow: 1;
    margin-bottom: var(--space-4);
  }

  .highlight-card__link {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-primary);
    transition: color var(--transition-fast);
  }

  .highlight-card:hover .highlight-card__link {
    color: var(--color-primary-dark);
  }

  /* About Preview Section */
  .about-preview {
    padding: var(--space-12) 0;
    background: linear-gradient(135deg, var(--color-surface) 0%, var(--color-background) 100%);
  }

  .about-preview__content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .about-preview__title {
    font-size: var(--text-3xl);
    font-weight: 700;
    margin-bottom: var(--space-6);
    color: var(--color-text);
  }

  .about-preview__text {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    line-height: 1.8;
    margin-bottom: var(--space-4);
  }

  .about-preview__text:last-of-type {
    margin-bottom: var(--space-6);
  }

  /* Music CTA Section */
  .music-cta {
    padding: var(--space-12) 0;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: var(--color-text-inverted);
  }

  .music-cta__content {
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
  }

  .music-cta__title {
    font-size: var(--text-3xl);
    font-weight: 700;
    margin-bottom: var(--space-4);
  }

  .music-cta__text {
    font-size: var(--text-lg);
    opacity: 0.9;
    margin-bottom: var(--space-6);
  }

  .music-cta__icon {
    margin-right: var(--space-2);
  }
</style>
